export const TAXONLIST_DISPLAY_MODE = Object.freeze({
  detailedList: "detailedList",
  gallery: "gallery",
  hybrid: "hybrid",
});

export const WIDGET_TYPE = Object.freeze({
  default: "default",
  list: "list",
});
