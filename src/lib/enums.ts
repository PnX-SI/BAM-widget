export enum TAXONLIST_DISPLAY_MODE {
  detailedList = "detailedList",
  gallery = "gallery",
  hybrid = "hybrid",
}

export enum WIDGET_TYPE {
  mapList = "mapList",
  list = "list",
  swipe = "swipe"
}
